<pc:PropertyControlBase x:Class="NiVE3.OpenFX.Bridge.Property.Control.OfxStringPropertyControl"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                        xmlns:local="clr-namespace:NiVE3.OpenFX.Bridge.Property.Control"
                        xmlns:pc="clr-namespace:NiVE3.Plugin.Property.Control;assembly=NiVE3.Plugin"
                        xmlns:ucv="clr-namespace:NiVE3.UI.Converter;assembly=NiVE3.UI"
                        mc:Ignorable="d"
                        x:Name="Root"
                        DataContextChanged="Root_DataContextChanged"
                        d:DesignHeight="450" d:DesignWidth="800">
    <pc:PropertyControlBase.Resources>
        <ucv:BooleanInvertConverter x:Key="BooleanInvertConverter"/>
    </pc:PropertyControlBase.Resources>
    <Grid>
        <TextBox x:Name="ValueTextBox"
                 Width="{Binding Property.TextBoxWidth}"
                 Margin="5,2,0,2"
                 HorizontalAlignment="Left"
                 VerticalAlignment="Center"
                 IsReadOnly="{Binding Property.IsReadOnly}"
                 IsEnabled="{Binding ParentLayerIsLock, Converter={StaticResource BooleanInvertConverter}}"
                 LostFocus="ValueTextBox_LostFocus"
                 KeyDown="ValueTextBox_KeyDown">
            <TextBox.Style>
                <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Property.IsMultiLine}" Value="True">
                            <Setter Property="AcceptsReturn" Value="True"/>
                            <Setter Property="AcceptsTab" Value="True"/>
                            <Setter Property="Height" Value="120"/>
                            <Setter Property="VerticalContentAlignment" Value="Top"/>
                            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
                            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Style>
        </TextBox>
    </Grid>
</pc:PropertyControlBase>
